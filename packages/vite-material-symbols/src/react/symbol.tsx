import { get_icon } from '../font';
import { iconMapping, supportedExports } from '../mappings';

type MaterialSymbolProps = {
  weight?: number;
}

export const MaterialSymbol = ({ weight = 400 }: MaterialSymbolProps) => {
  return (
    <div>

    </div>
  );
}
